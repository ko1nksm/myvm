do_build() {
  vm.build --name "$DOMAIN" --osinfo freebsd13.1 \
    --vcpus 1 --memory 2048 --disk size=10 \
    --network bridge='br0',mac='52:54:00:00:01:54' \
    --cdrom "$DISK_DIR/FreeBSD-13.1-RELEASE-amd64-disc1.iso" \
    --graphics vnc,listen='0.0.0.0' --noautoconsole

  waitfor "Boot Multi user"
  enter
}

do_setup() {
  waitfor "Welcome to FreeBSD"
  sendkey "I"

  waitfor "keyboard map"
  enter # default keymap

  waitfor "Please choose a hostname for this machine"
  enter "$HOSTNAME"

  waitfor "Choose optional system components to install"
  enter

  waitfor "How would you like to partition your disk"
  enter {DOWN}

  waitfor "Would you like to use this entire disk"
  enter # Entire Disk

  waitfor "Select a partition scheme"
  enter # MBR

  waitfor "Please review the disk setup"
  enter

  waitfor "commit your changes"
  enter

  waitfor "Please select a password"
  enter "$ROOT_PASS" "$ROOT_PASS"

  waitfor "Please select a network interface"
  enter

  waitfor "configure IPv4"
  sendkey y

  waitfor "DHCP to configure this"
  sendkey y

  waitfor "configure IPv6"
  sendkey y

  waitfor "stateless address"
  sendkey y

  waitfor "Resolver Configuration"
  enter

  waitfor "Select a region"
  enter 0 # UTC

  waitfor "Does the abbreviation 'UTC' look reasonable"
  sendkey y

  waitfor "Month Year"
  enter # Skip

  waitfor "Set Time"
  enter # Skip

  waitfor "Choose the services you would like to be started at boot"
  enter

  waitfor "Choose system security hardening options"
  enter

  waitfor "users to the installed"
  sendkey y

  waitfor "Add Users"
  enter "$USER_NAME"

  waitfor "Full name"
  enter "$USER_FULL_NAME"

  waitfor "Uid"
  enter

  waitfor "Login group"
  enter

  waitfor "Invite user into other groups"
  enter

  waitfor "Login class"
  enter

  waitfor "Shell"
  enter

  waitfor "Home directory"
  enter

  waitfor "Home directory permissions"
  enter

  waitfor "Use password-based authentication"
  enter # yes

  waitfor "Use an empty password"
  enter # no

  waitfor "Use a random password"
  enter # no

  waitfor "Enter password"
  enter "$USER_PASS"

  waitfor "Enter password again"
  enter "$USER_PASS"

  waitfor "Lock out the account after creation"
  enter # no

  waitfor "OK? (yes/no)"
  enter yes

  waitfor "Add another user"
  enter no

  waitfor "Setup of your FreeBSD system is nearly complete"
  enter

  waitfor "The installation is now finished"
  enter

  waitfor "installed system now"
  sendkey R # Reboot

  vm.vnc disable
}

spelling_correction() {
  sed '
    s/Helcome/Welcome/g
    s/Houw/How/g
    s/DK?/OK?/g
    s/nouw/now/g
  ' | tr '"' "'"
}
